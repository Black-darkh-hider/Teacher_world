# Dependencies
# These can be reinstalled from package.json, so no need to commit them.
/node_modules
backend/node_modules
frontend/node_modules

# Build output folder for the frontend
frontend/dist

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
# Keep these secret and do not commit them to a public repository!
.env
.env.local
backend/.env
frontend/.env